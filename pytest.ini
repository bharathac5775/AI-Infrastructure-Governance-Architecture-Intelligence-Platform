[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: full-pipeline tests (~100ms each)
    integration: end-to-end sample tests
